import 'package:flutter/material.dart';

class disp extends StatefulWidget {
  const disp({super.key});

  @override
  State<disp> createState() => _dispState();
}

class _dispState extends State<disp> {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("data"),
    );
  }
}
